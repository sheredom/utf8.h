language: cpp

compiler:
  - gcc
  - clang

env:
  - CONFIGURATION=Debug
  - CONFIGURATION=Release

script:
  - env
  - echo $CONFIGURATION
  - mkdir build
  - cd build
  - cmake -DCMAKE_BUILD_TYPE="$CONFIGURATION" ..
  - make
